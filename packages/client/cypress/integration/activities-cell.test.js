import { Checkbox } from '@doday/shared';
import React from 'react';
describe('Checkbox component', () => {
  it('works', () => {
    // mount the component under test
    cy.mount(<Checkbox />);
  });
});
