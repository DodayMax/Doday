export const BASE_ROUTES = {
  store: '/store',
  profile: '/profile',
  activities: '/activities',
};

export const STACKED_ROUTES = {
  dodays: '/dodays',
  progress: '/progress',
  builder: '/builder',
};

export const STATIC_ROUTES = {
  welcome: '/welcome',
};
